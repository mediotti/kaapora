version: '3'

services:
  app:
      build:
        context: .
        dockerfile: Dockerfile
      ports:
        - "4000:4000"
      volumes:
        - .:/usr/src/app
      command: /bin/sh -c "while sleep 1000; do :; done"